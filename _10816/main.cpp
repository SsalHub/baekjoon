#include <iostream>

typedef struct Node
{
    int value;
    int len;
    struct Node *next;
} Node;

int main()
{
    std::ios_base::sync_with_stdio(true);
    std::cin.tie(0);
    int N, M;
    std::cin >> N;
    return 0;
}