#include <iostream>

int main()
{
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(0);
    unsigned long long A, B, C;
    std::cin >> A >> B >> C;
    std::cout << A + B + C << "\n";
    return 0;
}