#include <iostream>
#include <vector>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int n, m, v, a, b;
    vector<int> graph(n + 1);
    cin >> n >> m >> v;
    for (int i = 0; i < m; i++)
    {
        cin >> a >> b;
    }
    return 0;
}