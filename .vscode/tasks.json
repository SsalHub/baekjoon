{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build active file (clang)",
            "type": "shell",
            "command": "clang++",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        },
        {
            "label": "killProcess",
            "type": "shell",
            "command": "taskkill",
            "args": [
                "/IM",
                "main.exe",
                "/F"
            ],
            "problemMatcher": []
        }
    ]
}
