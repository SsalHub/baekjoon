#include <iostream>
#define SIZE 5

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int n[SIZE], i, j, k, mult;

    for (i = 0; i < SIZE; i++)
        cin >> n[i];
    
    cout << mult << '\n';
    return 0;
}